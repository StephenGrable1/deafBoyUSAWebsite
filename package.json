{
  "name": "deafBoy-express-server",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm run start",
    "server": "node server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "drip-nodejs": "^1.0.1",
    "express": "^4.16.2"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
